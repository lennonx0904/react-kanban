import React from "react";

const BoardTitle = props => {
  return <div className="board-title center-justify d-flex flex-column justify-content-center align-items-center">{props.boardTitle}</div>;
};

export default BoardTitle;
